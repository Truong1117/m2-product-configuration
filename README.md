# m2-product-configuration
The extension change position of the simple product on the configuration product and dynamics field when changing the product configuration option (example: SKU, name, description,...)
